// Primives: store lightweight values also an object but in other sense (boolean, bigint, string, null, undefined, number, symbol)
// Object: Store multiple different values -> {} {name: "Talha", age: "22"}, Takes resources
//

// Primitive is also an obeject to provide methods for strings and numbers

// String()
// Number() returns number
// new Number() is an object not recommended with new keyword

// str.toUpperCase() method  objectWrapper(str).toUpperCase()

// Object Wrapper for primitives that objectwrapper provides methods

// null undefined there is no method
